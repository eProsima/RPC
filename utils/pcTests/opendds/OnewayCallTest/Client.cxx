/*************************************************************************
 * Copyright (c) 2012 eProsima. All rights reserved.
 *
 * This copy of RPCDDS is licensed to you under the terms described in the
 * RPCDDS_LICENSE file included in this distribution.
 *
 *************************************************************************
 * 
 * @file Client.cxx
 * This source file shows a simple example of how to create a proxy for interface OnewayCallTest.
 *
 * This file was generated by the tool rpcddsgen.
 */

#include "OnewayCallTestProxy.h"
#include "OnewayCallTestRequestReplyTypeSupportImpl.h"
#include "exceptions/Exceptions.h"

#include <iostream>

int main(int argc, char **argv)
{
    OnewayCallTestProxy *proxy = NULL;
    
    // Creation of the proxy for interface "OnewayCallTest".
    try
    {
        proxy = new OnewayCallTestProxy("OnewayCallTestService", 0, 30000L);
    }
    catch(eProsima::RPCDDS::InitializeException &ex)
    {
        std::cout << ex.what() << std::endl;
        return -1;
    }

    // Create and initialize parameters.
    CORBA::Long  lo1 = 10;

    // Call to remote procedure "setLong".
    try
    {
        proxy->setLong(lo1);
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<setLong>: %s\n", ex.what());
        _exit(-1);
    }

    CORBA::Long long_ret = 0;

    try
    {
        long_ret = proxy->getLong();

        if(long_ret != 10)
        {
            printf("TEST FAILED<getLong>: Wrong values\n");
            _exit(-1);
        }
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<getLong>: %s\n", ex.what());
        _exit(-1);
    }

    CORBA::Boolean bo1 = true;       

    try
    {
        proxy->setBoolean(bo1);
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<setBoolean>: %s\n", ex.what());
        _exit(-1);
    }

    CORBA::Boolean bo_ret = false;

    try
    {
        bo_ret = proxy->getBoolean();

        if(bo_ret != true)
        {
            printf("TEST FAILED<getBoolean>: Wrong values\n");
            _exit(-1);
        }
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<getBoolean>: %s\n", ex.what());
        _exit(-1);
    }

    char *s1 = strdup("Ricardo");       

    try
    {
        proxy->setString(s1);
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<setString>: %s\n", ex.what());
        _exit(-1);
    }

    if(s1 != NULL) free(s1);

    char* s_ret = NULL;

    try
    {
        s_ret = proxy->getString();

        if(strcmp(s_ret, "Ricardo") != 0)
        {
            printf("TEST FAILED<getString>: Wrong values\n");
            _exit(-1);
        }
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<getString>: %s\n", ex.what());
        _exit(-1);
    }

    if(s_ret != NULL) free(s_ret);

    Structure st1;
    st1.dato = 10;
    st1.message = strdup("Jaime");

    try
    {
        proxy->setStruct(st1);
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<setStruct>: %s\n", ex.what());
        _exit(-1);
    }

    Structure st_ret;

    try
    {
        st_ret = proxy->getStruct();

        if(st_ret.dato != 10 ||
                strcmp(st_ret.message, "Jaime") != 0)
        {
            printf("TEST FAILED<getStruct>: Wrong values\n");
            _exit(-1);
        }
    }
    catch(eProsima::RPCDDS::Exception &ex)
    {
        printf("TEST FAILED<getStruct>: %s\n", ex.what());
        _exit(-1);
    }

    printf("TEST SUCCESFULLY\n");
    
    delete(proxy);

    _exit(0);
   
    return 0;
}
